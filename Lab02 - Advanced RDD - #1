# 분석 요건 - Find the top 10 airlines with the most flights

# 1.1. RDD 생성하고 자료 조회하기
from pyspark.sql import Row
from pyspark.sql.types import *

flightRdd=sc.textFile('wasbs://ghtestbigdata@ghtestdatastacc.blob.core.windows.net/HOL-Data/Lab01/flights.csv').map(lambda line: line.split(",")) 
flightRdd.take(3)

# flightRdd.collect() (주의) 실행하지 말 것. (해당 파일이 대용량으로 jupyter 내 조회 시간이 오래 소요됨)
